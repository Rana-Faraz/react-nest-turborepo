# Backend

Minimal NestJS skeleton after the backend reset.

## Current Scope

- `GET /health`
- global validation pipe
- permissive CORS bootstrap for local development

## Scripts

```bash
pnpm --filter backend dev
pnpm --filter backend build
pnpm --filter backend test
```

## Next Step

Define the new backend boundaries and ground rules before adding modules, persistence, or auth again.
